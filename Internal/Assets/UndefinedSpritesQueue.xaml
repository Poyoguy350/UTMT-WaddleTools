<Window x:Class="System.Windows.Window"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:UndertaleModTool;assembly=UndertaleModTool"
		xmlns:lib="clr-namespace:UndertaleModLib.Util;assembly=UndertaleModLib"
        xmlns:scol="clr-namespace:System.Collections;assembly=mscorlib"
        xmlns:cmod="clr-namespace:System.ComponentModel;assembly=WindowsBase"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d" Height="800" Width="800" MinWidth="600" MinHeight="600"
		Title="Sprites With Unknown Types!">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="2*" />
			<RowDefinition Height="8*" />
			<RowDefinition Height="100*" />
			<RowDefinition Height="6*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="8*" />
			<ColumnDefinition Width="100*" />
			<ColumnDefinition Width="8*" />
		</Grid.ColumnDefinitions>
		
		<TextBlock Grid.Row="1" Grid.Column="1">
			There are sprite(s) that the script couldn't easily identify what type it is, <LineBreak/>
			Please help it identify by manually setting it for each one! (Or it'll just default to "WaddleSpriteType.Sprite").
		</TextBlock>
		
		<ListBox Grid.Row = "2" Grid.Column="1" Name="UnknownQueue"
			Foreground="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"
            Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"
			HorizontalContentAlignment="Stretch" 
		>
			<ListBox.ItemTemplate>
				<DataTemplate DataType="{x:Type sys:String}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="50*" />
							<ColumnDefinition Width="50*" />
						</Grid.ColumnDefinitions>
						<TextBlock Text = "{Binding}" Grid.Column="0" Margin="3" />
						<local:ComboBoxDark Margin="3" Grid.Column="1" VerticalAlignment="Center" SelectedIndex="0" Name="SpriteTypeCombo">
							<sys:String>Sprite</sys:String>
							<sys:String>Background</sys:String>
							<!-- <sys:String>Font</sys:String> -->
						</local:ComboBoxDark>
					</Grid>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		
		<Grid Grid.Row="3" Grid.Column="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="75*" />
				<ColumnDefinition Width="25*" />
			</Grid.ColumnDefinitions>
			
			<local:ButtonDark Margin="8" Content="Confirm" Grid.Column="1" Name="ConfirmTypesButton" />
		</Grid>
	</Grid>
</Window>